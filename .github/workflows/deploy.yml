name: Deploy to Lightsail

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup SSH
      uses: webfactory/ssh-agent@v0.6.0
      with:
        ssh-private-key: ${{ secrets.LIGHTSAIL_SSH_KEY }}

    - name: Ensure deployment directory exists
      run: ssh -o StrictHostKeyChecking=no ubuntu@18.117.28.235 'mkdir -p /home/ubuntu/integracionwiseconn'

    - name: Copy files via SCP
      run: scp -r -o StrictHostKeyChecking=no ./* ubuntu@18.117.28.235:/home/ubuntu/integracionwiseconn

    - name: SSH and run task scheduler
      run: |
        ssh -o StrictHostKeyChecking=no ubuntu@18.117.28.235 << 'EOF'
        cd /home/ubuntu/integracionwiseconn
        # Si usas un entorno virtual:
        source venv/bin/activate
        # Mata cualquier instancia en ejecución del task_scheduler.py
        pkill -f 'python3 task_scheduler.py'
        # Ejecuta task_scheduler.py
        nohup python3 task_scheduler.py &
        EOF
