# maneja las rutas y las vistas de Flask
# En views.py, podrías tener:

#Una ruta para obtener información de todas las zonas (GET /zones).
#Una ruta para agregar una nueva zona (POST /zones).
#Una ruta para actualizar una zona existente (PUT /zones/<id>).
#Una ruta para eliminar una zona (DELETE /zones/<id>).
#Cada una de estas rutas estará vinculada a una función que realiza las operaciones necesarias: consultar la base de datos, modificar datos, o simplemente devolver información.

